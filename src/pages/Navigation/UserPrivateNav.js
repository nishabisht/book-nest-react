import React from "react";
import PublicNav from "./PublicNav";

const UserPrivateNav = ({ menu }) => {
  return (
    <>
      <PublicNav menu={menu} />
    </>
  );
};

export default UserPrivateNav;
